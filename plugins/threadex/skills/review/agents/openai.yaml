interface:
  display_name: "Review"
  short_description: "Review diffs for bugs, regressions, and test gaps"
  default_prompt: "Use $threadex:review to inspect this change for evidence-backed ship blockers and residual risk."
policy:
  allow_implicit_invocation: true
