interface:
  display_name: "Verify"
  short_description: "Check completion claims against concrete evidence"
  default_prompt: "Use $threadex:verify to map every requirement to current evidence and determine whether this work is complete."
policy:
  allow_implicit_invocation: true
